<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dental.clinic.module.rolepermission.mapper.RolePermissionMapper">

    <!-- ResultMap for Permission, assuming it's defined in PermissionMapper.xml -->
    <!-- <resultMap id="PermissionResultMap" type="com.dental.clinic.entity.Permission"> ... </resultMap> -->

    <select id="selectPermissionsByRoleId" parameterType="java.lang.Long" resultMap="com.dental.clinic.module.rolepermission.mapper.PermissionMapper.PermissionResultMap">
        SELECT p.* FROM permissions p
        JOIN role_permissions rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND p.deleted = 0 AND p.status = 1
    </select>

    <select id="selectByRoleIdAndPermissionId" parameterType="java.lang.Long" resultMap="com.dental.clinic.module.rolepermission.mapper.PermissionMapper.PermissionResultMap">
        SELECT p.* FROM permissions p
        JOIN role_permissions rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND rp.permission_id = #{permissionId} AND p.deleted = 0 AND p.status = 1
        LIMIT 1
    </select>

    <insert id="insert" parameterType="map">
        INSERT INTO role_permissions (role_id, permission_id) VALUES (#{roleId}, #{permissionId})
    </insert>

    <delete id="deleteByRoleIdAndPermissionId" parameterType="map">
        DELETE FROM role_permissions WHERE role_id = #{roleId} AND permission_id = #{permissionId}
    </delete>

    <!-- <delete id="deleteByRoleId" parameterType="java.lang.Long">
        DELETE FROM role_permissions WHERE role_id = #{roleId}
    </delete>
    <delete id="deleteByPermissionId" parameterType="java.lang.Long">
        DELETE FROM role_permissions WHERE permission_id = #{permissionId}
    </delete> -->

</mapper>